*
    box-sizing border-box
    font-family Tahoma, Arial

:root
    font-size 16px

:root, html, body
    margin 0
    padding 0

// Global class names need to be nested under the :global block
:global
    .row
        display flex
        align-items center

        > * + *
            margin-left .5rem
